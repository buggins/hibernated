language: d

sudo: required
dist: trusty

d:
  - dmd
  - dmd-2.069.2
  - dmd-2.068.2
  - ldc-1.0.0-beta2
  - gdc

os:
 - linux

script: dub test --config=SQLite

addons:
  apt:
    packages: [ libsqlite3-dev ]

matrix:
  allow_failures:
    - d: gdc
